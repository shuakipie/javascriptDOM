const h1 = document.querySelector('h1')
h1.style // check the style
h1.style.color // target color to change the style
h1.style.color = 'blue' // change the color to inline blue on the DOM
p.style.backgroundColor = 'black' // this how to target backgroundColor